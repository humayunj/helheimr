# Helheimr metadata API server
Designed for the deployment on (heroku)[http://heroku.com/]

## Running locally
Use `yarn install` or `npm install` to install the packages.
and run using `yarn start` or `npm start`